#ifndef __STRUCTURE_GRID_TYPES_H__
#define __STRUCTURE_GRID_TYPES_H__

// the size of cells in any StructureGrid and RoomTypeGrid, since there is no reason that two StructureGrids would need different cell sizes, we just use a constant value
constexpr static float structureGridCellSize = 64.f;

#endif